# Global React Hooks (`src/hooks`)

## Responsibility
Shared, application-level state hooks used across multiple decoupled visual features.

## Boundary Rules
- **Allowed Imports**: Standard React Hooks, browser API bindings, and type systems.
- **Forbidden Imports**: Feature-specific views or feature-bound state models.
- **Layer Owner**: Application Layer.
