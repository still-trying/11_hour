# Firebase Architecture Layer (`src/firebase`)

## Responsibility
Standard web SDK clients, configurations, and Firestore queries, accompanied by database security schemas.

## Folder Contents
- `/rules/`: Contains local, deployable `firestore.rules`.
